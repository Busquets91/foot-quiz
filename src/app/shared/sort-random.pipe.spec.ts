import { SortRandomPipe } from './sort-random.pipe';

describe('SortRandomPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRandomPipe();
    expect(pipe).toBeTruthy();
  });
});
